[project]
name = "agents-everywhere"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = [
    "httpx>=0.28.1",
    "openai>=3.13.0",
    "opencv-python>=4.13.0.92",
    "python-dotenv>=1.2.3",
    "structlog>=26.1.0",
]

[dependency-groups]
dev = [
    "pytest>=9.1.1",
]
